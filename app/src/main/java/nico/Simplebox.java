package nico;

import android.os.Bundle;
import android.support.annotation.Nullable;
import android.support.v7.app.AppCompatActivity;

/**
 * Created by stytool on 2017/9/8.
 */

public class Simplebox extends AppCompatActivity {//主页


    //ThemeDialog dialog = new ThemeDialog();
    // dialog.show(getSupportFragmentManager(), "theme");

    @Override
    protected void onCreate(@Nullable Bundle savedInstanceState) {
        super.onCreate(savedInstanceState);
        /*
        setContentView(R.layout.activity_55main);

        final Toolbar mToolbar = (Toolbar) findViewById(R.id.toolbar);
        setSupportActionBar(mToolbar);
        if (getSupportActionBar() != null) {
            getSupportActionBar().setDisplayHomeAsUpEnabled(true);
        }
        mToolbar.setSubtitleTextAppearance(this, R.style.ts);

        TextView ndt = (TextView) findViewById(R.id.iko);
        ndt.setOnClickListener(new View.OnClickListener() {
            @Override
            public void onClick(View v) {
                Intent inten = new Intent(Simplebox.this, Main4Activity.class);
                startActivity(inten);
            }
        });
    }*/
    }
}